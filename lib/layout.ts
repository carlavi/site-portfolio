// Shared container widths so every page aligns to the same grid.
export const TEXT_WIDTH = "max-w-2xl"; // prose / reading columns
export const MEDIA_WIDTH = "max-w-5xl"; // hero, images, page container
